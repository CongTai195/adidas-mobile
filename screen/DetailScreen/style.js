import { StyleSheet } from "react-native"
const styles = StyleSheet.create({
    title: {

    },
    price: {
        fontSize: 18,
        fontWeight: 'bold',
        marginTop: 10
      }
});

export default styles;